# Image Classification MVP Example

Placeholder example workspace for the first supported end-to-end scenario.

The image-classification route is the current primary MVP path. Use the canonical contracts in `../../shared/contracts/` as schema-valid scaffolds:

- `data_contract.example.yaml`
- `model_contract.example.yaml`
- `deploy_contract.example.yaml`

Generated artifacts for an actual workspace should normally use unsuffixed operational names such as `data_contract.yaml`, `model_contract.yaml`, and `deploy_contract.yaml`.

Current project health check as of 2026-05-07: package tests pass with `199 passed` when run from `torch-skill-suite/`.
