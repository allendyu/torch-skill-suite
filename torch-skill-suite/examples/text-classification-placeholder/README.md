# Text Classification Placeholder

Placeholder example workspace for a later expansion scenario.

The current MVP validation route is image classification. Text-classification support exists in tests and templates, but this example workspace remains a placeholder until a runnable scenario is added.

When this example is implemented, keep the same contract naming convention used elsewhere: checked-in scaffold examples may use `*.example.yaml`, while generated workspace artifacts should use names such as `data_contract.yaml`, `model_contract.yaml`, and `deploy_contract.yaml`.
